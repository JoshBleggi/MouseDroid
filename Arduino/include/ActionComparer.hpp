// #ifndef ACTIONCOMPARER_H
// #define ACTIONCOMPARER_H

// #include "Movement.hpp"

// class ActionComparer {
//     public:
//         static bool IsForward(Movement* movement);
//         static bool IsReverse(Movement* movement);
//         static bool IsRotateClockwise(Movement* movement);
//         static bool IsRotateCounterClockwise(Movement* movement);
//         static bool IsUnstick(Movement* movement);
//         static bool IsDance(Movement* movement);
// };

// #endif